use 
npx prisma generate
npx prisma db push

in any designs that you make, do a button pass local to the design or functionality you made. meaning:
  if a button or ui should have a cursor pointer, do so.